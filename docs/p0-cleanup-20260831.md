# P0 第一阶段优化发布记录

日期：2026-08-31。通过独立工作区 Git 合并上线，先 checkpoint 保存线上其他人的 P0 改动。

- 优化合并：8f893b55；部署前基线：13fd4f79。
- 八个页面仅移除 lx-boot-guard 临时诊断，逐字节对比确认无额外页面变化。
- 802 个备份及 AppleDouble 文件，80,399,798 字节，已归档；当前公开目录 963 个文件、105,531,843 字节（采集时点）。
- 旧诊断接收入口 /_lxdiag 返回 410；Nginx 语法校验与平滑重载通过；未重启应用。
- 发布检查：961 个包含文件、2 个文档排除，五频道 JS/CSS 直接依赖完整。

归档：`/opt/projects/lexiang/backups/p0-public-cleanup-20260831-1130`。manifest.json 保存每个历史文件的哈希；entries-at-merge 是当时八入口原文；before-merge-hashes.json 为归档前全站哈希；nginx.before/after 与 nginx-result 记录配置变更。

回滚须先检查是否存在后续更新。代码通过独立工作区 revert 优化合并（主线 parent 1），处理冲突后再合并，禁止拿整份旧文件覆盖。历史文件仅在目标不存在且归档哈希匹配时按 manifest 恢复；Nginx 仅逆向应用 p0-nginx-diagnostic.patch 的目标区块，校验 nginx -t 后平滑重载。无需为回滚复制用户数据库或密钥。

本阶段未进行视觉重构、模块合并或按需加载，也不承诺首屏性能已提升。后续每个模块单独测试并发布。
