# P0 order status correctness fix — 2026-08-31

Baseline: d00d0139. Candidate verification: 13 browser tests with real DOM/MutationObserver, in-memory synthetic storage and controlled timers; four channel runtime fragment tests for stable card/payment identity. No private browser storage, real orders, backend payment or user data were accessed.

Changes:
- Startup schedules read-only rendering, never reconciles payment. Captured pending order must have an ID, match an existing completed demo payment, and be recent before metadata enrichment. Unknown ID never falls back to first order.
- Explicit status enumerations take precedence over legacy paidAt. No substring match for unpaid; no fabricated payment transaction number. Zero payable is retained. Identical metadata and DOM values are not rewritten.
- New pending cards bind data-lx-order-id at creation; the QR confirmation reuses this ID. Four existing runtime copies receive the same narrowly checked edits, without altering card layout, generation sequence or navigation.
- Paid result cards upgrade only for matching order IDs. Legacy cards lacking an ID remain untouched, rather than borrowing unrelated payment state. window owns the orders-updated listener.
- Preserve order center count and ID matching; do not replace filtered/demo cards by array index.

Limits: payment remains an existing browser-local demo; this is not a real payment integration. Legacy paidAt-only orders remain supported; no guessed historical repair or bulk migration occurs. Full end-to-end shopping/model/backend and full five-channel performance tests are not claimed. Existing old cards without IDs cannot be safely auto-upgraded. Source JS/CSS beyond the six changed scripts is preserved.

Tests: serve repository root locally and open tests/p0-orders/index.html; it uses isolated iframe documents with in-memory storage. Run node tests/p0-orders/test-runtime.cjs for runtime ID propagation. Neither suite reads real orders or invokes payment APIs. Syntax checks and p0-release validator are required before merge. Direct CSS/JS fingerprints are regenerated by scripts/p0-assets.py.

Deployment: merge isolated branch dev/guanjf2-p0-orders-20260831 after verifying production hashes, clean shared index and P0 scope; retain backup outside public. No backend restart or nginx change. Rollback through a scoped git revert of this commit and resource fingerprint verification; never roll back user orders.
