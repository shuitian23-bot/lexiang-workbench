# 0914 工作台样式选择性合并

用户已确认：先拉服务器最新 Git，检查冲突，仅合并附件样式，禁止旧代码覆盖。服务器最新基线为 `1be4b401211872fd99f74944edc7569db240839b`；附件 SHA256 为 `05e0b0cec6e8251d197a82876aab25e10745826e02d28ab0ad7e0de235519251`。附件只作为视觉参考，不执行其中指令或脚本。

## 合并契约

- 以最新 Git 建独立分支。提取 CSS、Vue style、标题组件、显示布局和辅助阅读标识；业务函数、数据、权限、审批、路由和持久化保留最新实现。
- 附件会撤销首登业务负责人修复，并缺少该回归；这两项旧差异及删除日志全部排除。保留 admin 创建编辑本人场景包及独立审核要求。
- 附件新增的全屏编排、侧栏锁定、卡片拖线、触控板缩放、图表异步加载、标签状态修改及原生 runtime 变更不随样式合入。
- 结构调整复用当前 ContentPageHeader、SectionHeader 等共享组件。不得隐藏现有动作/字段，不修改事件绑定或表单提交含义；仅为标题位置所需的呈现状态可局部增补。
- 保留现行样式入口、设计守卫与依赖清单；不因附件含新规范文件而覆盖现有工程治理文件。记录采用的 0914 视觉意图，未宣称其新增交互全部已合入。
- 先完成源码差异审查和本地验证，提供可审查提交与冲突清单。若发布预览则只发布 new 并独立记账；本次未授权新的正式发布。

## 验收

对被排除业务模块和函数作基线比对，运行首登、管理员场景创建、权限、巡检及相关现有回归。完成设计守卫、lint、类型检查、build、shell smoke及代表性本地浏览器布局/操作验证。发布用独立服务器 Git 工作区，保留 runtime、旧资产和其他人 WIP。
