# 权限管理源码选择性合并

基线：2abf15ff2c86c3cc2a54e13c0f55cb77e655598d，包含当前场景包编排、试运行重试、独立审批等改动。用户要求保留现有改动，仅调整权限管理。

附件：leaibot-workbench-fork-dev-guanfeng2.zip。对比显示权限业务逻辑已包含在当前版本；新增差异主要是权限组件控件高度、字号、间距与颜色变量。附件同时包含旧版本回退：删现有弹窗独立滚动、层级标签、发布记录、状态避让和业务负责人选择文案，均不采纳。

实施：在现有独立工作区的新权限分支中，按区块合并 permissions 组件样式，保留脚本和模板。主页面仅采用与现有规范一致的局部变量和颜色调整；更新同一权限记录。保留现有 modalOverflowState/adaptiveFooter，不引入重复滚动机制，不合并附件全局样式、路由、Skill 管理、场景包、依赖配置或服务端源码。

验证：对比基线，允许变化仅权限组件、权限页及该模块发布日志和计划。执行已有权限回归、工程检查及权限关键弹窗真实浏览器检查；场景包回归保留证明。构建只发布 new，保护正式站与 admin-runtime，归档源码范围和发布证据。
