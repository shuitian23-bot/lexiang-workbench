# Skill Hub 更新失败示例实施记录

**目标：**按用户要求，补齐已有文档中的“查看失败原因”可操作示例，先发布 new。

**依据与范围：**已有能力上下文受控更新方案要求保留失败原因、阶段、任务标识和重试建议；查看不改变状态，重试复用任务，忽略仅关闭当前变化，完整刷新恢复初始数据。此次实现这些已定义行为，不设计新的审批或查询流程。最新服务器基线为 `bd585e3d2c1e788fd1981add5c9096706bdd1804`。

**实现：**新增独立 `capability-update-failure-demo`，中文名“运营查询（更新失败示例）”，初始为已发布 v1.0.0 加更新失败。原有 15 条 Skill 及六条待决策变化保持。错误详情沿用现有弹层和样式，仅补可选阶段、错误码与建议字段。此示例重试使用明确标注的本地结果，通过现有 begin/complete 转换进入更新草稿 v1.0.1，线上版本保持 v1.0.0；其他 Skill 仍调用原模型服务。

## 执行清单

- [x] 核对文档、现有状态机及最新主线；原有 50 项能力更新测试通过。
- [x] 先验证初始示例缺失，再补种子与限定到专用示例的本地恢复结果；测试克隆隔离、任务复用、忽略、刷新和非示例隔离。
- [x] 注册独立列表记录；错误详情补充阶段、码、原因和建议，保持原有按钮权限及历史记录兜底。
- [x] 在本次更新任务入口使用专用示例结果，普通 Skill 请求方式不变；刷新重建示例，不写业务后端。
- [x] 本地完整 lint、typecheck、设计 Skill 接入检查、构建通过；700 项测试通过（新增 23 项）。1280/1440 浏览器验证查看、关闭、重试、返回、忽略及刷新，无真实模型或业务请求。
- [x] 增加一条功能调整日志。限定修复已忽略的初始失败任务仍拦截提交的问题；后续变化失败、生成中、非负责人及低分门禁保持。
- [ ] 服务器隔离构建与 Shell 冒烟后，按精确资源清单发布 new；发布记录单独归档实际版本、时间、资源和浏览器验证证据。

**设计检查说明：**增量扫描报告创建页已有的 273 条样式问题；两处修改页面的全部 style 块与最新基线逐字相同，因此本次没有新增样式违规。沿用现有弹层和错误提示样式，未扩大到历史样式整改。

## 验收路径

Skill Hub → 搜索“更新失败示例” → 查看失败原因 → 重试更新 → 查看带演示标识的恢复结果 → 返回列表 → 更新中。刷新再次出现更新失败；忽略后恢复已发布，再刷新仍可重复演示。查看原因不改变线上版本和列表状态。产品知识问答、券包权益推荐及其他原示例不发生变更。

## 发布边界

本轮仅更新 new 预览。源码推送到服务器个人预览分支以便隔离构建，不触发 incoming 自动合并；正式发布与 main 合并留待用户确认。运行文件、其他页面及真实业务数据不属于本轮范围。
