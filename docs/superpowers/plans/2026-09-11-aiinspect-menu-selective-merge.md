# AI 巡检菜单选择性合并计划

**目标：** 仅合入附件 AI 巡检菜单（数据总览、巡检规则、问题处理、通知记录、运行日志），保留权限管理、场景包和通用功能。

**基线：** c707a9aad9a91a0583e3a2fb96fff91a786e3ac0；new 入口指纹 bf1829d3be6c8deac24dfb45eb3a510a537ed0d508ba320243bbccf8203b0168。

## 范围和接口

- 新增 views/aiinspect 的 5 个页面、components/aiinspect 的 6 个专属组件、services/aiInspect.ts 模拟数据。
- app.ts 和 router/index.ts 仅追加 AI 巡检菜单、路由登记和专属可见性接线；保留全部已有菜单和守卫规则。管理员判断在域内实现，不导入附件通用权限改造。
- 页面复用当前 ContentPageHeader，不修改共享组件；仅在 AI 巡检范围内适配样式和弹窗生命周期，避免全局 body 状态污染。
- 附件其他页面、全局 CSS、公共组件、权限源文件、包依赖和构建产物全部排除。通知/日志仍按附件仅管理员展示；无真实业务接口或通知发送。
- 发布记录放在 AI 巡检域内；发布仅 new，保留 admin-runtime 和正式目录。

## 步骤

- [x] 导入专属源码并适配现有公共组件接口。
- [x] 增量登记 AI 巡检菜单和 5 个路由，验证旧菜单与守卫不变。
- [x] 验证页面筛选、详情、模拟复检、规则编辑和管理员可见性；只操作模拟数据。
- [x] 运行设计检查、lint、类型检查、构建、页面 smoke 及受影响范围回归。
- [x] 比对源码指纹，排除无关源码变化；独立审查后提交至 new 专用预览分支。
- [ ] 发布前检查基线，备份并增量更新 new，核验构建清单及正式/运行时目录未变，归档。
