# 场景包试运行精简 Implementation Plan

**Goal:** 删除试运行介绍区和测试任务填写，按前面场景及节点配置生成运行依据，节点结果紧接操作栏。

**Architecture:** 复用节点结果组件标题与 actions 插槽；默认请求由场景字段和解析后的节点执行顺序生成。创建页同步当前配置，保留节点模拟选项与历史报告；只读审核不改历史依据。

**Tech Stack:** Vue 3、Pinia、领域 JS、Node test、Vite。

- [x] 领域请求覆盖场景名称、描述、人群与节点任务、固定要求、预期输出和条件；补充内容/顺序/变更失效测试。
- [x] 创建页在可编辑状态同步运行依据，保留样例与确认选项，配置修改后须重跑。
- [x] 删除重复试运行标题/描述/灰块和输入框；保留紧凑模拟标识与节点结果、运行操作、错误提示。
- [x] 验证节点流程、readonly、窄工作区、工程与专项回归。
- [ ] 仅发布 new：沿用场景日志记录、完整构建清单、受保护目录核验和可回退入口切换；权限管理/正式环境不变。
