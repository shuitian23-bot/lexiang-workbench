# 场景包仅展示运行结果 Implementation Plan

**Goal:** 第三步只查看各 Skill 的执行反馈、上下游传递和错误，不重复展示或编辑前序配置。

**Architecture:** Summary 使用 resultsOnly 展示模式；提交和审核默认保留详细模式。Trial 仅保留显式确认/条件分支的按需重试动作，不默认确认；取消手工样例覆盖并使其旧报告过期。

- [x] 精简第三步配置、依据、预期/版本和测试设置回显；保留结果、错误、节点选择与返回修改。
- [x] 必要确认和条件分支改为直接重试动作；缺可用样例时准确报错，不伪造成功。
- [x] 可编辑草稿清理隐藏样例覆盖；历史记录与只读审核不变。测试运行锁、重跑和独立审核。
- [x] 专项回归、工程检查、本地视觉/交互检查通过。
- [ ] 明确范围提交并仅发布 new；发布证据归档于工作区交付记录。
