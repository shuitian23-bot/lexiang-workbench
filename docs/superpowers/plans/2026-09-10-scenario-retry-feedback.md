# 场景包第二轮试运行修复

用户最新要求：首次展示正反 mock，返回编辑后再次试运行应成功，不再重复演示错误。

已复现：职场人群经营分析 → 职场认证状态查询。返回编辑后重跑执行了，但输入仍选择同一不完整 fixture，结果完全相同；只有 nextTick 的同步运行让加载状态不可见。

修复范围：场景包试运行请求新增持久化 retry 阶段，上一轮已有 mixed-feedback 报告时点击重试进入完整样例；新建仍从初始正反样例开始。请求规范化、报告指纹、提交 gate 使用同一阶段。权限、版本、确认、实际配置等原有检查保留。重试显示运行中，并在完整样例通过后提示可提交。

验证：先用真实 Vue/Pinia 流程和 domain 回归复现旧行为，再实现；覆盖返回修改、返回编排、上一步和页签导航，新建重置、请求持久化、重试全成功、重复点击与真实阻塞。完成工程检查、本地浏览器和 new 资源校验；仅发布 new，保护正式站与 admin-runtime。
