# Four-channel member/store loader recovery

Baseline 868d4998. Extend the already tested homepage recovery to shop-chat, b-chat, biz-chat and brand runtime loaders. Each runtime is a standalone closure; insert the exact same private helper and narrowly replace two Promise executors. No new global helper or resource/load order is introduced. Existing tests/p0-loader-recovery/test.cjs accepts a runtime path and tests the actual functions. No UI redesign or design-system modification.

Contract: preserve each channel's exact original member JS/CSS and store JS URLs, including brand's older CSS query version; preserve CSS insertion, bridge, mount arguments, active-tab/disconnected guards, error/loading markup and store detail event handling. Add 8000ms script timeout, failed script/listener cleanup and reset both promise caches on rejection. Concurrent entries share one script request. Feature re-entry is the retry path; no new button or automatic retry loop. CSS recovery and cancellation of underlying network transport are not covered. Same-host mount calls are not deduplicated.

Evidence: 22 real-function synthetic DOM/timer cases per channel. Each baseline passes 11/22 and candidate 22/22 (88/88 overall). Cases cover error and timeout retry, absent registration, concurrent reuse, late events, cleanup and view guards. Browser fixture runs actual candidate function blocks with only script/CSS URLs redirected to inert local resources. All eight channel/feature combinations recover from a real first HTTP503, then three concurrent entries share one successful script request. Server counts are exactly two per combination (failed initial + successful retry). Stub mount merely counts calls, so this is loader lifecycle verification, not authenticated feature rendering/E2E.

Deployment: patch latest server worktree source only, assert baseline and exact replacements, regenerate static entry fingerprints, check syntax and whitelist; preserve homepage and all unrelated files. Shared lock + unchanged production checks + outside-public backup + scoped Git merge. Check five entries/eight redirects/all referenced resource headers after release. Rollback via scoped Git revert followed by fingerprint validation, never by copying a local site snapshot.
