# Replace two standalone pages with current feature entrypoints

Baseline59ccf342. Add a small shop-only script in head before other initialization because existing app clears location.search. Capture whitelisted p0entry=stores/member early; on DOMContentLoaded invoke existing __lxOpenFeature, retry registration up to8s, at most one invocation. No submission, auth bypass or new component implementation. Other query values and pages do nothing. Current member/style/store load recovery remains unchanged.

New entry URLs: /shop-chat/?p0entry=stores and /shop-chat/?p0entry=member. Nginx exact302 maps old /assets/pages/shop-chat-appointment-offline.html and /shop-chat/member-stage.html to those routes, appending original args. Fixed route is first so original duplicate p0entry cannot override. Browser retains URL fragment through HTTP redirect; existing app subsequently normalizes address as before. No claim to migrate old standalone browser state or make parameterized legacy store selection equivalent. Offline standalone operation is retired; current HTTP site is the replacement.

Local browser verification uses a fresh current-site snapshot, in-memory storage, denied geolocation and mocked API fetch responses. Current real UI rendered store list, appointment confirmation form (not submitted), and member center. No backend/account operations performed. Eight routing tests cover early query capture, both features, unknown/missing/wrong-path no-op, duplicate parameter priority, delayed registration and bounded timeout. Authenticated completion/result not tested; it remains existing feature behavior. Old member stage dependency files intentionally retained.

Deploy additive JS and shop HTML first, then exact nginx rules with config hash check, backup, nginx-t and reload. Verify old redirects before deleting two physical HTML files via second scoped Git merge. Backup both originals and server config outside public. If permissions leave physical old files after Git deletion, remove only hash-verified manifest leftovers with existing sudo capability, no permission changes. Validate remaining files and resource fingerprint coverage. Update merged changelog both phases.

Rollback needs reverse Git changes and only the two added nginx locations, validated against latest config; do not replace unrelated later config changes. Current canonical HTML/runtime and old HTML originals are backed up. nginx snapshot in docs is release evidence, not authority to overwrite future config.
