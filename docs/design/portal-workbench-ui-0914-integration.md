# 0914 样式 Skill 项目接入

本次按用户提供的 `portal-workbench-ui-0914.zip`，在已合并样式的最新预览源码上应用设计规则。源包 SHA-256 为 `84d8f8844aa1d57d2e56fa47f4c24c9c92c93fc3201d19e8c5dbd0dcd1ff0999`；与前一份源码包随附的 0914 Skill 共 83 个文件逐字一致。

## 规则与源码边界

- `skill/portal-workbench-ui-0914` 为用户原件的完整分发副本；不修改、删减或另外编写开发版、PM 版 Skill。
- `skill.meta.json` 的发布号为 0914，设计合同继承 0908。正文、代理显示名和 manifest 中保留的 0908/0824 文案属于原包历史信息，不以这些文字替代当前项目版本。
- 用户已确认的当前需求优先，然后应用 0914 设计合同。附包的页面登记、VA-R2 截图只表示它的原始审阅范围，不代表本项目今天全部页面、状态、键盘或后端已经验收。
- 首登 `AccessDeniedView` 未列入附包矩阵，继续保留当前业务负责人五项选择、必填校验、申请快照、审批链和旧申请兼容逻辑。缺失登记不表示应删除当前功能。
- 管理员保留创建与编辑本人场景包的能力；四步创建、固定版本、试运行拦截、禁止自审与其他管理员审核保持当前实现。样式 Skill 不改变角色能力。
- 路由、状态、服务、数据、运行文件与封板壳层行为不在此次样式修正范围。历史 Skill 目录只作对照，不作为本轮默认入口。

## 组件映射与修正

本项目已有且验证过的 `ContentPageHeader`、`SectionHeader` 和语义 token 继续作为真实运行实现，Skill 目录不作为运行时组件包或样式包导入。

| 页面或模式 | 本轮应用方式 |
| --- | --- |
| 协议订单 T1 | 删除覆盖共享页头的局部字号/行高/间距；复用原 19px/12px 共享组件，筛选到表格统一 12px。 |
| Skill Hub T7 | 主视图页签采用文字与下划线；指标卡间距使用既有 14px 语义 token。 |
| 场景编排 T4 + V2 | “已加入”采用选择状态的 primary token，不再借用 warning 告警色；专项三栏内部滚动保持。 |
| AI 巡检列表 | 去除追加的 13px 表头覆盖，恢复现有 12px 表头，正文仍为 13px。 |
| 指标卡 | 本轮追加的卡片圆角统一为已定义的 `--radius-md`。 |
| 政企线索 T2 + V1 | 页头与同排工具栏到首个业务块统一为 16px；窄宽布局的独立操作行保留。 |

## 工程应用

根 `AGENTS.md`、Vue README、Skill 搜索路径与建议版本统一指向 0914。已有设计检查入口接入随仓库原版一致性检查，开发和构建保留原产品回归；未知参数、缺失检查文件和合同检查失败必须返回非零，不能仅打印成功。

原始一致性检查可以验证 Skill 元信息、资产、模板、路由与矩阵；不传变更文件时不宣称页面样式已通过。`--changed-file` 实际检查指定文件的整个 style 块，包含这些文件的旧样式债务。必须保留原输出，区分本轮改动与未修改的历史声明，不为清零修改固定的共享 19px 页头，也不把历史债务冒充本轮已验收。

图标 18px、标题 19px、副标题 12px、标题间距 3px、页头到首区块 16px，以及页签和状态语义，通过项目实际页面核验。未触达历史页面的整体迁移不在此次接入范围。

## 发布与验证

工程基线为服务器预览分支 `2eb218219e5733dad69d6408bad8b1d2123b1917`，正式 main 为 `1be4b401211872fd99f74944edc7569db240839b`；发布前再次读取服务器状态。只按当前授权更新 new，沿用 `workbench-ui0914-style-20260915` 同一调整记录，环境的人员、时间和源码版本在真实发布后登记。

保留原源码、历史产物和共享运行文件；提交、构建、部署均在隔离工作区完成。交付记录区分规则静态检查、增量样式审阅、自动回归、本地交互和公开资源核验。
