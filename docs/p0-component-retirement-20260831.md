# Remove 21 retired component resources

User requested the first four categories from the latest candidate report: 14 old store JS files, five old member JS files, one old member CSS fix and the store-v5-embed.js.gz companion. Exact paths and hashes are in the manifest. Baseline a04635a4; no HTML files or active resources are removed. Preserve appointment offline page, member-stage test page and their dependencies. Preserve current member-service-clean-v45.js, store-v5-detail-surface-v27.js, all store media, current five runtimes and active styles.

Previously no full filename references found in P0 text assets or the supplementary tracked-code scan (excludes docs/tests/skills/symlinks and >8MB files). Same production baseline confirmed and each byte hash rechecked before deletion. Global gzip_static is enabled; remove retired store-v5-embed.js and its gzip companion together. Companion decompression differed from sibling source, but no rewrite or diagnosis of active serving is attempted. Static scans do not prove absence of external links or dynamically built paths. Removed URLs intentionally return404.

Archive originals outside public at backups/p0-component-retirement-20260831/before/public/leaip0/ with parent directory0700, validate backup hashes, then scoped Git merge from isolated worktree under shared lock and latest production/hash/index checks. Update merged daily cleanup changelog. Validate remaining site release/fingerprints, five page entries, eight redirects, current resource responses and removed404 responses. All surviving public site file hashes must remain unchanged. No current feature E2E or speedup claim. Rollback through a scoped Git revert after checking latest production state; never copy a whole old site over production.
