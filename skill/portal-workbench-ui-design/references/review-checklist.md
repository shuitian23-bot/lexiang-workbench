# 交付前评审清单

## 目录

1. Token
2. 品牌与字体
3. 布局与响应式
4. 组件
5. 乐享项目专项
6. 可访问性与文案
7. 主题状态
8. Skill 沉淀
9. Skill 前向回归场景

## 1. Token

- [ ] 本次 PM、UI 或研发工作使用 `portal-workbench-ui-0803/` 作为唯一当前规范源；涉及通用内容槽时读取了 `content-slot-design-contract.md`、`page-spec-coverage-matrix.md` 和 `responsive-layout-contract.md`。
- [ ] 当前迭代以 0803 Vue 项目为当前设计和执行基线；0729 及更早项目仅作历史非回归对照，没有用历史旧版项目或旧 skill 覆盖当前规则。
- [ ] 颜色全走 `--color-*` 语义变量，无规范外硬编码色。
- [ ] 字号来自字号阶梯；正文 14px，表格 / 表单 13px。
- [ ] 间距使用 4px 基准 Token。
- [ ] 控件高度走 `--control-height-*`。
- [ ] 圆角、阴影、层级、动效使用统一 Token。

## 2. 品牌与字体

- [ ] Logo 未变色、未拉伸、未裁切。
- [ ] 左上品牌区符合当前封板：展开态只显示红色 `联想乐享` 完整 logo，不显示 `工作台` 胶囊；logo 约 `112x23px`，展开态收起按钮 `28x28px` 且与 logo lockup 约 `3px` 间距；收起态 icon mark `36x36px`、rail handle `24x24px`。
- [ ] 深浅背景使用正确 Logo 版本。
- [ ] 侧栏展开 / 收起态 Logo 形态合理。
- [ ] 字体栈符合 `typography.md`，技术内容使用等宽字体。

## 3. 布局

- [ ] 对当前 0803 Vue 项目已有页面做逐页对比：页面 ID、标题、导航激活、内容区主要结构、KPI / 表格 / Tabs / 筛选数量与当前项目一致；差异只能来自明确保留的新功能。
- [ ] 如本次为外部 POC / 历史功能合入，已以上一个已确认项目版本为底座，做文件级对比并选择性合入；最终实现必须落到 Vue 源码，不引入独立 HTML/JS，不整包覆盖当前项目；没有误删 AI 页面巡检、门户首页、本地预览、静态页签、Topbar AI 结果选择器与中间结果视图、AI 队列和等待态能力。顶部搜索按当前规则默认隐藏但保留必要 DOM。
- [ ] 已确认 `portal-workbench-ui-0803` 用于约束新增需求，或明确提出的既有功能优化/修改需求的设计和实现一致性；本次 POC 对已确认项目的功能、设计、交互、样式、布局、路由或壳层规则的改动都已标注为有意变更，并说明范围和影响。
- [ ] 改源码前已完成 PM 需求前置确认：列出本次新增内容、优化/修改的已有功能、影响的已确认内容、不应改动的页面/功能/交互/样式/架构，以及需要明确标注的有意变更，并经确认后才开始实现。
- [ ] 使用统一 App Shell。
- [ ] 若修改的是壳层、页签、AI 面板、Auth、路由或 Store，设计/实现能映射到 Vue 组件、Pinia、Router、composable/service，不新增散落的全局 DOM 桥接。
- [ ] 若新增或迁移 Vue 组件，组件专属样式与组件对应，写在该 `.vue` 的 `<style lang="scss" scoped>`；没有把组件私有样式继续追加到集中式全局 CSS。
- [ ] 页面已归入一个明确页型模板。
- [ ] 已记录 `viewport / sidebar / agent / contentInnerWidth`；四列筛选 `>=1040px`、两列 `720–1039px`、单列 `<720px`，并按页型专项阈值验收。
- [ ] `1040/720px` 只控制基础栅格密度，页面仍 `width:100%` 流式伸缩；静态模板和新页面没有使用 `920/620px` 作为平行的通用断点，表格最小宽度等同数值规则未被误改。
- [ ] 当前浏览器截图、测量、finding、remaining states 和复核时间已写入 `page-visual-acceptance.json`；页面矩阵的 VA 状态与机器记录一致。
- [ ] VA-R2 只用于两组响应式初始态证据通过；只有交互、数据边界、键盘/无障碍和工程命令也通过时才使用 VA-PASS。失败与阻塞保留为 VA-FAIL / VA-BLOCKED。
- [ ] Container Query 挂在页面 padding 内层且自身无水平 padding 的布局容器；记录的 `contentInnerWidth` 与该容器的 `getBoundingClientRect().width` 一致，没有因外层 `24px` padding 让断点提前触发。
- [ ] 已覆盖 Agent 关、默认 `380px`、最大约 `492px`；页面根 `scrollWidth-clientWidth=0`，必要横向滚动只发生在表格容器。
- [ ] 数据密集型内容槽流式撑满；只有阅读/表单型内容使用有理由的受控最大宽度；内边距、栅格与间距节奏正确。
- [ ] 缩屏时裁切顺序一致：中间内容槽先压缩或换列，再收起左侧导航，最后才约束右侧 Agent；普通导航页和 Skill 创建 / 需求澄清页没有不同的提前裁切行为。`1280px` 初始加载已验证侧栏自动收起为 `58px` 且页面级横向溢出为 `0`；低于项目最小宽度时只允许壳层全局滚动，不允许页面内部工作区先溢出。
- [ ] 复杂两栏/三栏页面没有使用刚性主列最小宽度导致内容槽提前溢出；主工作区使用 `minmax(0,1fr)` 或等价弹性写法，grid/flex 子项允许 `min-width:0`。
- [ ] 顶层页面区块使用 `24px`、同一区块内模块使用 `16px`、普通卡片内边距使用 `20px`；KPI 组合只在其局部使用 `12/14/16px` 紧凑节奏。筛选区、表格、详情卡之间有明确分隔，不贴边、不重叠。
- [ ] 每页都有页面标题；描述和页面级主操作按任务需要出现，不为空页面强行补齐。
- [ ] 所有需要横向对齐的卡片、表格、图表在同一行顶部一致、等高一致。
- [ ] 所有需要纵向对齐的标题、按钮、筛选器、表格列、图表容器在同一基线上。
- [ ] 模块标题保持左上角横排展示；当筛选器/按钮放不下时，控件换到下一行，标题没有被压缩成竖排或错位。
- [ ] 模块标题区满足双边对齐：标题组贴左，说明 / 筛选 / `问 AI` / 操作按钮贴右；换行时操作组整体换行，不漂浮在中间。
- [ ] 标题不强制添加装饰方块、竖线或圆点；存在已确认语义图标/装饰时，与标签和右侧按钮保持同一视觉中线。
- [ ] 多行标题区如沿用已确认装饰，装饰锚定第一行标题，不按整个 header 高度居中。
- [ ] Grid 子项没有用额外 `margin-top` 造成错位，间距由 grid gap 或统一 section spacing 控制。
- [ ] 侧栏展开态每个父级目录单行展示；chevron 收起向右、展开向下；独立业务域已拆分为独立父级目录。

## 4. 组件

- [ ] 按钮层级清晰，一个页面通常只有一个主按钮。
- [ ] 按钮、图标按钮、Tab、筛选 pill 高度稳定，文字不变形、不挤压、不意外换行。
- [ ] 按钮 / chip / 时间范围 pill 同组间距一致：图文 6px，同组 8px，横向内边距 12px；不用临时空格或随意 margin 修正位置。
- [ ] 表单标签在上，错误提示清晰。
- [ ] 表格有加载、空、错误状态。
- [ ] 状态标签同状态全站同色。
- [ ] 危险操作有二次确认。
- [ ] 历史会话删除等高影响对话操作有行内二次确认或撤销入口，不能一键直接删除。
- [ ] Toast、Alert、Modal、Drawer 符合组件规范。
- [ ] 图表卡与表格卡同排时，内容区自适应且不空旷；图表已在布局变化后重新计算尺寸。
- [ ] 图表颜色使用单色阶或协调中低饱和色板，类别辨识度足够，同一语义跨图表颜色一致，强红只用于真实警示状态。
- [ ] 全站中间数据展示区的页面顶部 KPI / 主要数据卡片使用基于主色推导的蓝、青、靛、紫四种极浅辅助渐变循环，有色辅助色 alpha 低于 10%（推荐 2.5%-8%），白色收尾可为 100%，颜色有很轻的信息感但不明显抢眼；下方链路步骤 / 拆解项 / 二级统计块 / 图表容器 / 表格容器 / 筛选区 / 待接口占位块保持白底；未使用普通分类浅红、语义 wash、重复标签圆点、左侧色条或顶部色条。
- [ ] 本次样式调整已覆盖所有相关业务模块，不只覆盖“乐享运营”或某一个示例页面。
- [ ] Tabs 选中态只使用文字色 + 下划线，不使用整块背景高亮；Tabs 和筛选区有明确间距，不贴边、不重叠、不靠临时内联样式撑开。
- [ ] Tabs 点击后能切换 active 状态和内容面板，不存在“只显示 tab 样式但点击无反应”的静态 tab。
- [ ] Tabs 的点击逻辑切换标准 `.active` class，并清理冲突内联样式；不是仅修改 `style.color` 或下边框颜色。
- [ ] 描边线框型模块 / chip / 次按钮在 hover 或 selected 时使用主色描边和轻微主色外环；页面级 Tab 不套用描边选中态。
- [ ] 中间数据区域所有卡片 / 面板 / 图表卡 / 表格容器 / 链路节点 / 占位卡使用统一轻量 hover：主色浅描边、轻阴影、`translateY(-1px)`、`.14s` 过渡；AI 面板、按钮、Tab、表格行和输入框未误套用该效果。
- [ ] 联动筛选型指标卡（如 `认证方式分布`）有 hover、focus、selected、清除状态，使用 `aria-pressed`，点击后下方列表/表格确实同步变化，并在列表标题行显示当前筛选条件。
- [ ] 当前强制浅色状态下，筛选 chip / 时间范围 / 分段按钮的选中、未选中和禁用态对比清晰；未来明确恢复深色时再执行深色专项验收。
- [ ] 页面顶部筛选按钮、时间范围、上传/刷新/下载等操作保持单行，不出现按钮文字换行；自定义日期范围使用浮层或等价不挤压布局的方案。
- [ ] 日期时间输入和下拉选择器使用默认 36px 高度、`--radius-md`（8px）圆角、白底细边框和主色 focus 外环，没有内外双重重边框。
- [ ] `AI 解读` / `问 AI` 按钮带小 icon 和主色浅渐变重点样式，不与普通次级按钮混同。
- [ ] 图标按钮 tooltip 在顶栏、AI 面板头部、输入区、抽屉内均可见，不被 overflow 或边界裁切。
- [ ] 下拉菜单、日期时间选择器、筛选 Popover 的圆角、边框、阴影、hover 和选中态符合组件规范。
- [ ] 新增功能按钮没有使用 emoji 前缀；导入、导出、新建、删除、返回等常规操作使用标准按钮文案或统一线性图标。
- [ ] 新增模块样式有页面级作用域或最终 override 约束，没有把 POC 私有 class / inline 视觉污染旧页面。
- [ ] Skill Hub 的技能包启停、批量申请、业务审批人和应用测试入口符合本地 token、当前浅色主题和紧凑控件规范。

## 5. 乐享项目专项

- [ ] 左侧导航顺序、父级目录、子菜单、SVG 图标体系、chevron 展开逻辑与当前 0803 Vue 项目一致；新增入口不得插入或打乱现有业务导航。
- [ ] 当前确认版左侧导航不展示 `首页` 父级目录；点击左上 Logo 可回到门户首页，面包屑和 AI 助手关注胶囊可同步到 `首页 / 联想门户工作台`。
- [ ] 门户首页本期不展示 `今日工作流 / 核心域联动` 面板；四张能力卡、常用入口和流程卡保持白底/细边框/轻 hover 风格。
- [ ] 当前可见业务页均使用 Vue route/page layer；隐藏详情页可保留 legacy 兼容。搜索后台、风控管理已从第一版封板项目删除，不应保留旧路由或占位页。新增功能页面使用当前项目组件/样式拼装，不单独发明一套卡片/表格/图标风格。
- [ ] 右侧 AI 助手使用当前 0803 Vue 完整交互：历史会话、范围标签横向滚动 + 左右圆形箭头、附件、发送/停止切换、队列展示、欢迎态、结构化回复、继续执行动作和当前内容槽关注胶囊；范围标签不退化为下拉。队列清空或停止完成后没有 `running` / `pending` 状态残留。
- [ ] API Key 默认掩码并提供复制操作。
- [ ] ID / Key / 数字 / 日志 / JSON 使用等宽字体。
- [ ] 日志级别颜色统一。
- [ ] 配额接近上限使用 warning，满额或失败使用 danger。
- [ ] 复杂创建流程使用 Steps / Wizard。
- [ ] AI 助手默认宽度为 `380px`；支持拖拽扩宽，最大 `492px`，到达最大时只自动收起由 AI 触发的左侧导航，往回拖恢复；头部开关、标签区、输入区、附件和发送按钮符合右侧助手规范；快捷标签位于输入区上方，默认无选中项，点击后才高亮。
- [ ] AI 输入框默认 `36px`，输入增多时增长到 3 行后内部滚动；发送按钮保持单个 `32x32px` icon-only 控件，没有双 icon 重叠。
- [ ] 左侧导航顶部、中间区顶部、AI 区顶部高度一致；AI 助手头部关注胶囊与当前左侧导航二级标题同步，不硬编码某个页面名，也不拼接父级目录。
- [ ] AI 助手头部保留 4 个 icon-only 操作，顺序为 管理技能 / 新开会话 / 历史对话 / 收起 AI 助手，第三个是历史对话。
- [ ] AI 助手默认态为互动文档流欢迎语：`你好！我是乐享 AI 助手。...`，并只展示紧凑推荐问题卡；不出现本期不上线的今日速览、指标卡、异常卡或最近对话。
- [ ] AI 长篇数据 / 解读报告只在右侧显示统一结论卡片，不把全文堆在 AI 面板；点击 `展开报告` / `展开查看` 后通过 Topbar 单一 AI 结果选择器激活对应结果，完整报告渲染到中间内容槽，且支持保存、关闭、多开对比和返回当前静态页面。
- [ ] AI 外链和 HTML 预览不会新开窗口；外链通过 AI 结果选择器进入链接视图并提供复制链接，HTML 预览进入 iframe 结果视图并支持下载 HTML；不生成第二排动态页签。
- [ ] 静态页签和当前会话 AI 结果各自最多 10 个；超出上限时按打开顺序关闭最早的同类对象并给轻提示。
- [ ] 当前 0803 Vue 版代码变更保持 `pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm smoke:shell` 通过。
- [ ] AI 等待回答态是透明状态行，不是 assistant 气泡；无白底、边框、圆角或阴影。
- [ ] AI 正式答案使用 assistant 气泡，包括纯文本、报告结论、任务结论和继续执行动作；过程状态不伪装成答案，TODO 作为答案后的独立直接列表且无气泡/卡片背景，授权卡等条件性结构输出保持自己的语义容器。
- [ ] Agent UAT / POC 状态补齐并封板：TODO List、多步骤同时进行、授权操作确认卡均遵循“过程区 → 答案气泡 → 条件性结构输出”的信息层级；批准前不执行高影响操作，且没有影响 Agent 头部、composer、队列、面板宽度、Skill 创建底部操作区或壳层裁切顺序。
- [ ] AI 助手和 Skill 创建 / 需求澄清的状态图标均为统一线性 SVG；状态 icon 容器里没有 `◇`、`⌁`、`↝`、`?` 等字符符号残留。
- [ ] 乐享运营 5 个菜单仍在 Vue route/page layer：运营总览、Query 分析、质量分析、流量分析、GMV 分析没有被重新路由回 `NativeWorkbenchPage`，且既有图表颜色、图例间距、入场动效、AI 气泡、响应式规则未回退。
- [ ] 后续中间内容槽深化只针对当前封板范围：GEO 看板、在职员工管理、企业客户管理和 Agent 页面；搜索后台、风控管理如重新需要，必须按新需求重新新增。每迁移一个大目录前后都执行“已确认基线不回退验收”，确认所有已确定的组件化边界、shell 行为、Agent 行为、图表规范、视觉样式、路由归属、业务功能和响应式裁切顺序没有被新改动影响，而不是只检查新迁移页面。
- [ ] 客户留言和企业客户管理 / 线索管理已作为独立业务目录进入侧栏，页面功能点未丢失，列表、抽屉、筛选、操作状态齐全。
- [ ] 企业客户管理当前 0803 Vue 子项包含 `线索看板`、`线索池`、`打分模型`；线索看板、线索池、打分模型均可打开并保留角色视图、筛选、表格/图表、审批和规则配置能力。

## 6. 可访问性与文案

- [ ] 焦点环明显且一致。
- [ ] 图标按钮有 Tooltip / 可访问名称。
- [ ] SVG、Logo、头像、图标、图表 canvas 没有被横向或纵向拉伸。
- [ ] Tooltip 展开方向符合所处位置：顶部向下、底部向上，且层级足够。
- [ ] 错误提示给出修复动作。
- [ ] 空状态说明原因并提供下一步。
- [ ] 数据格式统一：千分位、时间、百分比、ID 省略。

## 7. 主题状态

- [ ] 当前发布隐藏亮/暗切换入口，启动时强制浅色并清除或忽略历史深色偏好。
- [ ] `portal.home`、`dashboard.overview`、`pipeline.annotate`、`employee.certification`、`dashboard.geoKnowledge` 等代表页面没有历史深色背景残留。
- [ ] 新页面使用语义 token，不通过硬编码阻断未来主题能力。
- [ ] 只有用户明确恢复深色模式时，才启用完整深色专项验收：Logo、图表、表格、卡片、弹层、Agent 和所有内容槽状态均需重新验证。

## 8. Skill 沉淀

- [ ] 本次是否包含临时设计调整已记录清楚。
- [ ] 同类调整若已经在真实页面成功复用 `3-5` 次，已回写到对应 reference、Token 或 `assets/base.css`。
- [ ] 跨设备运行所需的相对路径、随包资产和模板仍完整可用。
- [ ] 已运行 `node scripts/check-consistency.mjs --project <当前项目目录>`，资产、模板、矩阵、截图和项目路由交叉检查通过。

## 9. Skill 前向回归场景

当修改根 `SKILL.md`、内容槽合同、页型、交互、响应式、架构或决策优先级时，使用互不共享预设答案的新会话/子任务执行以下场景；只改局部 token 或单一组件时可选受影响场景。测试任务不得先告诉执行者正确页型或预期结论。

每个测试结果必须明确列出：实际读取文件、主页面类型及理由、组件/例外、正常与边界状态、受保护表面、响应式测量组合、证据与仍需确认事项。缺少任一项不算通过。

| 场景 | 测试意图 | 通过重点 |
|---|---|---|
| 新标准列表 | 2–3 个常用筛选、行操作和批量选择 | 能选 T1，不因为有表格就默认 T2；含加载/空/错误/权限与表格滚动 |
| 复杂长筛选列表 | 8 个条件、高级筛选、批量操作 | 能选 T2；4/2/1 列阈值、条件保留、折叠与重置规则完整 |
| 数据看板 | KPI、双图、排行和时间筛选 | 能选 T3；图表 resize、空/错误/无权限等高状态与 Agent 组合完整 |
| 导入部分失败 | 上传、校验、异步处理、失败明细下载 | 能选 T5；排队/运行/部分成功/失败/重试/权限均被区分 |
| 长业务表单离开 | 两列长表单、校验、草稿、未保存退出 | 能选 T4；900px 降列、StickyActions、错误聚焦和离开保护完整 |
| 权限拒绝 | 页面、字段、操作和导出四级权限差异 | 不把禁用、无数据、无权限混为一种；高风险操作先确认 |
| Agent 极窄内容槽 | 1280 viewport、侧栏自动收起、Agent 默认/最大宽度 | 使用实际 `contentInnerWidth`，页面根零横向溢出，状态和输入不丢失 |
| 截图/Figma 走查 | 用当前页对比 Figma 04/UAT 历史快照 | 能把 04 当问题证据而非正确样式源；给出当前代码/截图证据和置信度 |
| PM POC 合并 | POC 同时新增业务能力、全局 CSS 和壳层重设计 | 保留真实业务增量，拒绝未授权壳层/全局覆盖，先列影响并等待确认 |

全量场景通过只证明 Skill 能指导决策，不代替真实浏览器截图、计算样式、键盘、接口与工程命令验收。任何失败都应优先修正现有合同中的歧义或路由缺口，不新增一份平行规范。
